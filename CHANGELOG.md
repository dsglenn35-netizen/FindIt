# Changelog

本项目所有值得记录的变更都会列在这里。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.2] - 2026-09-01

### 修复

- 🐛 **编辑记录时重拍/换照片后「保存」丢失照片**：此前拍照返回会先写库、点「保存」又用旧照片覆盖，现在照片变更统一在对话框内暂存、点「保存」一次性落库（先存无照片记录后补拍也一样）
- 📷 **首次打开自动请求相机权限**：zxing 扫码库把 CAMERA 权限带进了 manifest 却从不动态申请，导致必须去系统设置手动开；现在首次启动即弹权限请求，拍照/扫码前也会检查并按需请求（拒绝后再次操作会重新引导）

### 新增

- 🖼️ **图库选图**：记一笔 与 编辑对话框新增「图库」按钮，可从系统相册选照片（系统选择器，无需存储权限）
- 🗑️ **照片单独删除**：编辑对话框新增「删除照片」按钮（有照片时显示）；记一笔卡片的照片预览旁新增「移除照片」按钮
- 🧹 取消编辑或删除照片时会清理对应的临时照片文件，不留孤儿文件

### 界面

- 「记一笔」卡片重排：拍照/图库/预览一行，保存按钮独立成行（更大的点击区域）

## [2.1] - 2026-08-31

### 加固

- 🔐 **同步传输加密**：同步接口与照片全部改用 PIN 派生密钥加密（AES-256-GCM 信封 + AES-256-CTR 照片流），明文 HTTP 通道上不再裸传任何数据；PIN 同时作为接入凭证与密钥种子（PBKDF2-SHA256 65536 轮）
- 🛡️ **主机后台保活**：主机模式改为前台服务托管（`SyncHostService`），通知栏常驻 + 「停止主机」一键关闭；App 退后台/被划掉后主机继续运行，不再随界面销毁而停止
- 🗑️ **取消删除墓碑自动清理**：不再清理 30 天前的墓碑，避免离线设备把已删记录「复活」；家庭规模数据量小，墓碑永久保留成本可忽略
- 📦 **release 开启 R8 混淆**：APK 体积缩小，并补充 pinyin4j/NanoHTTPD/zxing 的保留规则

### 界面

- 列表行增加点击按压反馈
- 硬编码颜色/提示文本抽成资源（`text_on_primary` 等），便于统一主题维护
- 常用位置芯片加大触控高度

### 兼容性

- ⚠️ v2.1 与 v2.0 的同步协议不互通（加密封装变化），两台设备需同时升级到 v2.1+

## [2.0] - 2026-08-30

### 新增

- 🔗 **局域网双设备同步**：两台 Android 设备同一 WiFi 下互相同步记录、常用位置和照片
  - 任意一台可开「同步主机」模式，显示地址 + 二维码 + PIN，另一台扫码即连
  - 增量同步（只传变更）+ 冲突自动按"最后修改时间"裁决 + 照片按需下载
  - App 前台时每 30 秒自动探测同步，也可手动「立即同步」
- 📱 设备命名：同步页可给本机起名，方便识别

### 工程

- 数据库升级至 v3：新增 `updated_at` / `deleted`（软删除墓碑）/ `device_id` / `remote_id` 列，自动迁移
- 照片改为文件名存储（跨设备一致），老数据自动转换
- 新增依赖：NanoHTTPD（内置 HTTP 服务）、zxing core（二维码生成）、zxing-android-embedded（扫码）
- 许可证由 MIT 改为 **CC BY-NC 4.0（禁止商用）**

## [1.1] - 2026-08-30

### 新增

- 🎤 **语音输入**：说话自动填写物品名称/位置（调用系统语音，无需权限）
- ⚙️ **自定义常用位置**：位置芯片可增删改（长按重命名/删除，「+ 添加」新增）
- 🏠 **按位置分组浏览**：页签切换，按房间分组查看
- 📊 **位置统计图表**：每个位置放了多少东西一目了然
- 🔍 **拼音/模糊搜索**：输入 `jd` 也能搜到「剪刀」
- 🕘 **移动轨迹**：编辑时改位置自动记录历史（上次在哪 → 现在在哪）
- 💾 **备份导出/导入**：记录 + 照片打包成 zip，换机/卸载前导出可随时恢复
- 🔗 **分享单条记录**：一键把「东西在哪」发给家人
- 🖼️ **照片大图查看**：点列表缩略图全屏查看
- 🌙 **深色模式**：跟随系统自动切换

### 工程

- 数据库升级至 v2：新增拼音列 / 常用位置表 / 移动历史表（自动迁移，旧数据保留）
- 接入 GitHub Actions：推送自动构建 APK，打 tag 自动发布 GitHub Releases
- 发布英文版 README（README_EN.md）
- 签名密钥口令加强（签名证书不变，可覆盖升级）

## [1.0] - 2026-08-30

### 新增

- 📦 **记位置**：输入「物品名称 + 存放位置」一键保存
- 📷 **拍照记录**：给物品拍照
- 🕘 **最近记录**：按存放时间倒序排列
- 🏠 **常用位置快捷选择**：客厅/卧室/厨房等一键填入
- 纯本地存储（SQLite + 照片目录），**不需要任何权限、不联网、不上传**
